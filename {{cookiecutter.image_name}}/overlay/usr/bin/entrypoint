#!/usr/bin/env sh

# shellcheck disable=SC3040
set -eo pipefail

# shellcheck disable=SC1091,SC3046
source /usr/local/lib/log.sh

log_info "Start {{ cookiecutter.image_name | capitalize }}\n"
exec # <start command>
